

<h1><%= @client.name %></h1>
<h2>About <%= @client.name %></h2>
<p>
This will be some information about the company.  Do we need to put this in seeds?  Or maybe have this blurb stored somewhere else and somehow coded to the client name?  Anyways, I'm sure they're a great company and do lots of cool stuff and junk.
</p>
<h2>Stats</h2>
<ul>
<li>Hours Worked: <%= @client.hours %></li>
<li>Issues resolved: <%= @client.hours/2 %></li>
<li>Sandwiches eaten: <%= @client.hours/4 %></li>
</ul>
<h2>Websites</h2>
<div class='container-fluid'>
	<% @client.web_sites.all.each_slice(2) do |websites|%>
		<div class='row-fluid'>
			<% websites.each do |website| %>
				<div class='span6'>
					<h3><a href='<%= website.url %>'><%= website.url %></a></h3>
					<h4>Response Time</h4>
					<div id="rtime_<%= h(website.url) %>" style="width: 300px; height: 150px; border: 1px solid red">
						<script type="text/javascript">
							$(function () {
								$(document).ready(function() {
									bigchart("rtime_<%= h(website.url) %>", 
										<%= website.response_time_ms_1_minute_interval.to_json.html_safe %>
									);
								});
							});
						</script>
					</div>
					<h4>Requests per Minute</h4>
					<div id="rpm_<%= h(website.url) %>" style="width: 300px; height: 150px; border: 1px solid red">
						<script type="text/javascript">
							$(function () {
								$(document).ready(function() {
									bigchart("rpm_<%= h(website.url) %>", 
										<%= website.requests_per_minute.to_json.html_safe %>
									);
								});
							});
						</script>
					</div>
					<h4>Users</h4>
					<div id="users_<%= h(website.url) %>" style="width: 300px; height: 150px; border: 1px solid red">
						<script type="text/javascript">
							$(function () {
								$(document).ready(function() {
									bigchart("users_<%= h(website.url) %>", 
										<%= [Random.new.rand(0..20), Random.new.rand(0..20), Random.new.rand(0..20), Random.new.rand(0..20), Random.new.rand(0..20), Random.new.rand(0..20), Random.new.rand(0..20)] %>
									);
								});
							});
						</script>
					</div>
					<h4>Recent Deployments</h4>
					<div>
						<ol>
							<li>Deployment 1</li>
							<li>Deployment 2</li>
							<li>Deployment 3</li>
							<li>Deployment 4</li>
						</ol>
					</div>
				</div>
			<% end%>
		</div>
	<% end %>
</div>