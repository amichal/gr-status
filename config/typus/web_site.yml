# Use the README file as a reference to customize settings.

WebSite:
  fields:
    default: url, client_id, ga_profile_id
    form: url, client_id, ga_profile_id
  relationships: 
  application: Application
